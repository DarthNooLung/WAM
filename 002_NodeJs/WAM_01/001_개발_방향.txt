1. 최초 접속시 순번 부여하는 부분
    1) ActionList 생성
    2) ActionList 파일에 기록
    3) 접속자에 대한 쿠키 발행
        - 쿠키로 할지 파라미터로 할지 고민됨
            쿠키의 경우 한 브라우저에서 여러가지 탭을 사용할 경우 쿠키가 하나만 체크함
    4) 쿠키 발행에 대한 기록 <---- DB를 다녀오면 시간이 오래걸림, 어떻게 기록을 할껀지에 대한 고민 필요
        - ActionId / 쿠키값 / 순번 / 접속IP / 접속일시